#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include "phaseOne.h"

void assemble(FILE *fp) {
    /* read lines from fp */
    const char* line = "";

    fp = fp;

    /* for each line in lines: */
    /* phase1()... */

    processAssemblyLine(line);

    /* for each line in lines: */
    /* phase2()... */

}